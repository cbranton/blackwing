using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class playerCam : MonoBehaviour
{
    /*private GameObject playerPos = null;
    public float playerX = playerPos.transform.position.x;
    public float playerY = playerPos.transform.position.y;
    /*void Update(){
        if ((playerX < rightEdge) && (playerX > leftEdge)){
            cameraX = playerX;
        } 
    }*/
}
